@extends('layouts.app') @section('content')<div class="wrap"><div class="muted">Super Admin</div><h1>Control center</h1><div class="grid grid3"><div class="card"><div class="muted">Users</div><h2>{{ $users }}</h2></div><div class="card"><div class="muted">Invitations</div><h2>{{ $invitations }}</h2></div><div class="card"><div class="muted">Pending orders</div><h2>{{ $orders->where('status','pending')->count() }}</h2></div></div><h2 style="margin-top:30px">Order terbaru</h2><div class="card">@foreach($orders as $o)<div style="display:flex;justify-content:space-between;align-items:center;padding:10px 0;border-bottom:1px solid #eee"><span>#{{ $o->id }} · Rp{{ number_format($o->amount,0,',','.') }} · {{ $o->status }}</span>@if($o->status==='pending')<form method="post" action="{{ route('admin.orders.verify',$o) }}">@csrf<button class="btn">Verifikasi</button></form>@endif</div>@endforeach</div><h2 style="margin-top:30px">Moderasi foto</h2><div class="grid grid3">@foreach($photos as $p)<div class="card"><img src="{{ Storage::url($p->path) }}" style="width:100%;aspect-ratio:1;object-fit:cover;border-radius:12px"><b>{{ $p->guest_name }}</b><div style="display:flex;gap:8px;margin-top:12px"><form method="post" action="{{ route('admin.photos.moderate',[$p,'approved']) }}">@csrf<button class="btn">Approve</button></form><form method="post" action="{{ route('admin.photos.moderate',[$p,'rejected']) }}">@csrf<button class="btn danger">Reject</button></form></div></div>@endforeach</div></div>@endsection
