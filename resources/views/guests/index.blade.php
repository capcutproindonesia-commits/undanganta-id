@extends('layouts.app') @section('content')<div class="wrap"><div style="display:flex;justify-content:space-between;align-items:center"><div><div class="muted">{{ $invitation->title }}</div><h1>Daftar tamu</h1></div><a class="btn alt" href="{{ route('guests.export',$invitation) }}">Export CSV</a></div><form class="card" method="post" action="{{ route('guests.store',$invitation) }}">@csrf<div class="grid" style="grid-template-columns:2fr 1fr 1fr auto"><input class="input" name="name" placeholder="Nama tamu" required><input class="input" name="phone" placeholder="WhatsApp"><input class="input" name="category" placeholder="Kategori"><button class="btn">Tambah</button></div></form><div class="card" style="margin-top:18px;overflow:auto"><table style="width:100%;border-collapse:collapse"><tr><th align="left">Nama</th><th>RSVP</th><th>Link</th><th></th></tr>@foreach($guests as $g)<tr><td style="padding:13px 0">{{ $g->name }}<div class="muted">{{ $g->phone }}</div></td><td align="center">{{ $g->rsvp_status ?? '-' }}</td><td><input class="input" value="{{ route('public.invitation',['slug'=>$invitation->slug,'g'=>$g->token]) }}" readonly></td><td><form method="post" action="{{ route('guests.destroy',[$invitation,$g]) }}">@csrf @method('DELETE')<button class="btn danger">×</button></form></td></tr>@endforeach</table>{{ $guests->links() }}</div></div>@endsection
