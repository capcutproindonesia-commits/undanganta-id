@extends('layouts.app') @section('content')<div class="wrap"><div style="display:flex;justify-content:space-between;align-items:end;margin-bottom:22px"><div><div class="muted">Workspace</div><h1 style="margin:4px 0">Undangan saya</h1></div><a class="btn" href="{{ route('invitations.create') }}">+ Undangan baru</a></div><div class="grid grid3">@forelse($invitations as $i)<div class="card"><div style="display:flex;justify-content:space-between"><span class="pill">{{ strtoupper($i->plan) }}</span><span class="muted">{{ $i->views_count }} views</span></div><h2>{{ $i->title }}</h2><p class="muted">{{ $i->groom_name }} & {{ $i->bride_name }} · {{ $i->guests_count }} tamu</p><div style="display:flex;gap:8px;flex-wrap:wrap"><a class="btn" href="{{ route('invitations.edit',$i) }}">Edit</a><a class="btn alt" href="{{ route('guests.index',$i) }}">Tamu</a>@if($i->is_published)<a class="btn alt" target="_blank" href="{{ route('public.invitation',$i->slug) }}">Lihat</a>@endif</div></div>@empty<div class="card"><h3>Belum ada undangan.</h3><p class="muted">Mulai dari undangan pertama kamu.</p></div>@endforelse</div></div>@endsection
