<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $invitation->groom_name }} & {{ $invitation->bride_name }}</title><meta name="robots" content="noindex,nofollow"><style>body{margin:0;font-family:Georgia,serif;background:#f8f4ee;color:#312d28}.section{max-width:780px;margin:auto;padding:70px 22px;text-align:center}.cover{min-height:100svh;display:grid;place-items:center;background:linear-gradient(160deg,#ede5d8,#faf8f2)}h1{font-size:clamp(44px,9vw,80px);font-weight:400;margin:15px 0}.sans{font-family:system-ui,sans-serif}.btn{display:inline-block;padding:12px 18px;border-radius:999px;background:#302d29;color:#fff;border:0}.card{background:white;border-radius:20px;padding:20px;margin:14px 0}.input,textarea,select{width:100%;padding:12px;border:1px solid #ddd;border-radius:10px;margin:7px 0;box-sizing:border-box}.wishes{text-align:left}.muted{opacity:.65}</style></head><body><section class="cover"><div>@if($guest)<div class="sans muted">Kepada Yth. {{ $guest->name }}</div>@endif<div>The Wedding of</div><h1>{{ $invitation->groom_name }} <span style="font-size:.5em">&</span> {{ $invitation->bride_name }}</h1><div class="sans">{{ $invitation->event_date->translatedFormat('l, d F Y') }}</div></div></section><section class="section"><p>{{ $invitation->quote }}</p><h2>{{ $invitation->venue_name }}</h2><p class="sans muted">{{ $invitation->venue_address }}</p>@if($invitation->maps_url)<a class="btn sans" href="{{ $invitation->maps_url }}" target="_blank">Buka Maps</a>@endif</section>@if(data_get($invitation->sections,'story',true))<section class="section"><h2>Our Story</h2><p style="white-space:pre-line">{{ $invitation->story }}</p></section>@endif<section class="section sans"><h2>Konfirmasi Kehadiran</h2>@if(session('ok'))<div class="card">{{ session('ok') }}</div>@endif<form class="card" method="post" action="{{ route('public.rsvp',$invitation) }}">@csrf<input type="hidden" name="token" value="{{ $guest?->token }}"><input class="input" name="name" value="{{ $guest?->name }}" placeholder="Nama" required><select name="status"><option value="hadir">Hadir</option><option value="tidak_hadir">Tidak hadir</option><option value="ragu">Masih ragu</option></select><input class="input" type="number" name="party_size" min="1" max="10" value="1"><textarea name="message" placeholder="Ucapan dan doa"></textarea><button class="btn">Kirim RSVP</button></form>@if(in_array($invitation->plan,['premium','pro']))<h2 style="margin-top:60px">Bagikan foto</h2><form class="card" method="post" enctype="multipart/form-data" action="{{ route('public.photo',$invitation) }}">@csrf<input class="input" name="guest_name" placeholder="Nama" required><input class="input" type="file" name="photo" accept="image/*" required><input class="input" name="caption" placeholder="Caption"><button class="btn">Kirim foto</button></form>@endif@if($wishes->count())<h2 style="margin-top:60px">Ucapan</h2><div class="wishes">@foreach($wishes as $w)<div class="card"><b>{{ $w->guest_name }}</b><p>{{ $w->message }}</p></div>@endforeach</div>@endif</section><footer class="section sans muted">Made with UNDANGANTA.ID</footer></body></html>
