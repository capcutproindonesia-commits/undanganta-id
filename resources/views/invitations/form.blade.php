@extends('layouts.app') @section('content')<div class="wrap"><div style="display:flex;justify-content:space-between;gap:12px;align-items:center"><div><div class="muted">Invitation builder</div><h1>{{ $invitation->exists?'Edit undangan':'Undangan baru' }}</h1></div>@if($invitation->exists)<form method="post" action="{{ route('invitations.publish',$invitation) }}">@csrf<button class="btn alt">{{ $invitation->is_published?'Jadikan draft':'Publish' }}</button></form>@endif</div><form class="card" method="post" enctype="multipart/form-data" action="{{ $invitation->exists?route('invitations.update',$invitation):route('invitations.store') }}">@csrf @if($invitation->exists)@method('PUT')@endif<div class="grid" style="grid-template-columns:repeat(auto-fit,minmax(260px,1fr))"><div class="field"><label>Judul</label><input class="input" name="title" required value="{{ old('title',$invitation->title) }}"></div><div class="field"><label>Slug</label><input class="input" name="slug" value="{{ old('slug',$invitation->slug) }}"></div><div class="field"><label>Nama mempelai pria</label><input class="input" name="groom_name" required value="{{ old('groom_name',$invitation->groom_name) }}"></div><div class="field"><label>Nama mempelai wanita</label><input class="input" name="bride_name" required value="{{ old('bride_name',$invitation->bride_name) }}"></div><div class="field"><label>Tanggal acara</label><input class="input" type="datetime-local" name="event_date" required value="{{ old('event_date',$invitation->event_date?->format('Y-m-d\\TH:i')) }}"></div><div class="field"><label>Venue</label><input class="input" name="venue_name" required value="{{ old('venue_name',$invitation->venue_name) }}"></div><div class="field"><label>Alamat venue</label><input class="input" name="venue_address" value="{{ old('venue_address',$invitation->venue_address) }}"></div><div class="field"><label>Google Maps URL</label><input class="input" name="maps_url" value="{{ old('maps_url',$invitation->maps_url) }}"></div><div class="field"><label>Tema</label><select name="theme"><option>modern</option><option>minimal</option><option>classic</option><option>floral</option></select></div><div class="field"><label>Music URL</label><input class="input" name="music_url" value="{{ old('music_url',$invitation->music_url) }}"></div><div class="field"><label>Custom domain (Premium)</label><input class="input" name="custom_domain" value="{{ old('custom_domain',$invitation->custom_domain) }}"></div></div><div class="field"><label>Cover</label><input class="input" type="file" name="cover" accept="image/*"></div><div class="field"><label>Gallery (bisa banyak)</label><input class="input" type="file" name="gallery[]" accept="image/*" multiple></div><div class="grid" style="grid-template-columns:repeat(auto-fit,minmax(220px,1fr))"><div class="field"><label>Bank / E-wallet</label><input class="input" name="gift_bank" placeholder="BCA / DANA"></div><div class="field"><label>Nomor rekening</label><input class="input" name="gift_number"></div><div class="field"><label>Atas nama</label><input class="input" name="gift_name"></div></div><div class="field"><label>Quote</label><textarea name="quote">{{ old('quote',$invitation->quote) }}</textarea></div><div class="field"><label>Love story</label><textarea name="story" rows="6">{{ old('story',$invitation->story) }}</textarea></div><div style="display:flex;gap:16px;flex-wrap:wrap;margin:15px 0"><label><input type="checkbox" name="section_story" value="1" checked> Story</label><label><input type="checkbox" name="section_gallery" value="1" checked> Gallery</label><label><input type="checkbox" name="section_gift" value="1" checked> Amplop</label><label><input type="checkbox" name="section_wishes" value="1" checked> Ucapan</label></div><button class="btn">Simpan perubahan</button></form>@if($invitation->exists)<div class="card" style="margin-top:20px"><h2>Upgrade paket</h2><p class="muted">Paket aktif: <b>{{ strtoupper($invitation->plan) }}</b></p><div class="grid grid3">@foreach($plans as $plan)<form method="post" enctype="multipart/form-data" action="{{ route('orders.store',$invitation) }}" class="card">@csrf<input type="hidden" name="plan_id" value="{{ $plan->id }}"><h3>{{ $plan->name }}</h3><div style="font-size:24px;font-weight:900">Rp{{ number_format($plan->price,0,',','.') }}</div><input type="hidden" name="payment_method" value="transfer_manual"><div class="field"><label>Bukti transfer</label><input class="input" type="file" name="payment_proof" accept="image/*"></div><button class="btn">Pilih paket</button></form>@endforeach</div></div><div style="margin-top:20px;display:flex;gap:10px"><a class="btn alt" href="{{ route('guests.index',$invitation) }}">Kelola tamu</a><form method="post" action="{{ route('invitations.destroy',$invitation) }}" onsubmit="return confirm('Hapus undangan?')">@csrf @method('DELETE')<button class="btn danger">Hapus</button></form></div>@endif</div>@endsection
